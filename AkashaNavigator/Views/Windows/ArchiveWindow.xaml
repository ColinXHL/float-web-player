<helpers:AnimatedWindow x:Class="AkashaNavigator.Views.Windows.ArchiveWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:AkashaNavigator.Helpers"
        Title="归档管理"
        Width="500"
        Height="550"
        WindowStartupLocation="CenterScreen"
        Foreground="White"
        ResizeMode="NoResize">

    <helpers:AnimatedWindow.Resources>
        <!-- TreeViewItem 样式 (包含 EventSetter，必须本地定义) -->
        <Style x:Key="ArchiveTreeViewItemStyle" TargetType="TreeViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="6,6"/>
            <Setter Property="Margin" Value="0,1"/>
            <Setter Property="IsExpanded" Value="False"/>
            <EventSetter Event="PreviewMouseRightButtonDown" Handler="TreeViewItem_PreviewMouseRightButtonDown"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeViewItem">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Padding="{TemplateBinding Padding}"
                                    CornerRadius="4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <!-- 只有目录才显示展开按钮 -->
                                    <ToggleButton x:Name="Expander" Grid.Column="0"
                                                  ClickMode="Press"
                                                  IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                                  Visibility="{Binding DataContext.IsFolder, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolToVisibilityConverter}}"
                                                  Margin="0,0,4,0">
                                        <ToggleButton.Style>
                                            <Style TargetType="ToggleButton">
                                                <Setter Property="Focusable" Value="False"/>
                                                <Setter Property="Width" Value="20"/>
                                                <Setter Property="Height" Value="20"/>
                                                <Setter Property="Cursor" Value="Hand"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="ToggleButton">
                                                            <Border x:Name="ExpanderBorder" Background="Transparent" 
                                                                    Width="20" Height="20" CornerRadius="3">
                                                                <Path x:Name="Arrow" 
                                                                      Data="M 3,0 L 6,4 L 3,8 Z"
                                                                      Fill="#888"
                                                                      HorizontalAlignment="Center" 
                                                                      VerticalAlignment="Center"
                                                                      RenderTransformOrigin="0.5,0.5"/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsChecked" Value="True">
                                                                    <Setter TargetName="Arrow" Property="RenderTransform">
                                                                        <Setter.Value>
                                                                            <RotateTransform Angle="90"/>
                                                                        </Setter.Value>
                                                                    </Setter>
                                                                </Trigger>
                                                                <!-- Hover 效果：保持背景透明，仅改变箭头颜色和透明度 -->
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="ExpanderBorder" Property="Background" Value="#3A3A3A"/>
                                                                    <Setter TargetName="Arrow" Property="Fill" Value="White"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </ToggleButton.Style>
                                    </ToggleButton>
                                    <ContentPresenter x:Name="PART_Header" Grid.Column="1"
                                                      ContentSource="Header"
                                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                                </Grid>
                            </Border>
                            <ItemsPresenter x:Name="ItemsHost" Grid.Row="1" Margin="24,0,0,0"
                                            Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="True">
                                <Setter TargetName="ItemsHost" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#0078D4"/>
                            </Trigger>
                            <Trigger SourceName="Bd" Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#333"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#0078D4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </helpers:AnimatedWindow.Resources>

    <!-- 主容器带边框 -->
    <Border x:Name="MainContainer" Background="#1E1E1E" BorderBrush="#333" BorderThickness="1" CornerRadius="8"
            RenderTransformOrigin="0.5,0.5" Opacity="0">
        <Border.RenderTransform>
            <ScaleTransform ScaleX="0.96" ScaleY="0.96"/>
        </Border.RenderTransform>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Grid.Row="0" Background="#252525" CornerRadius="8,8,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="📁 归档管理" FontSize="14" FontWeight="SemiBold" 
                               Foreground="#EEE" VerticalAlignment="Center" Margin="16,0"/>
                    <Button Style="{StaticResource WindowCloseButtonStyle}"
                            HorizontalAlignment="Right" VerticalAlignment="Top"
                            Click="BtnClose_Click"/>
                </Grid>
            </Border>

            <!-- 内容区 -->
            <Grid Grid.Row="1" Margin="10,10,10,16" MouseLeftButtonDown="ContentArea_MouseLeftButtonDown">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 工具栏 -->
                <Grid Grid.Row="0" Margin="0,5,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- 搜索框 -->
                    <TextBox x:Name="SearchBox"
                             Grid.Column="0"
                             Style="{StaticResource SearchBoxStyle}"
                             Tag="搜索归档..."
                             TextChanged="SearchBox_TextChanged"/>

                    <!-- 创建归档按钮 -->
                    <Button Grid.Column="1" Content="📄 创建归档" Style="{StaticResource ToolbarButtonStyle}"
                            Margin="10,0,0,0" Click="BtnCreateArchive_Click"/>

                    <!-- 新建目录按钮 -->
                    <Button Grid.Column="2" Content="📁 新建目录" Style="{StaticResource ToolbarButtonStyle}"
                            Margin="6,0,0,0" Click="BtnNewFolder_Click"/>

                    <!-- 排序切换按钮 -->
                    <Button x:Name="BtnSort" Grid.Column="3" Content="↓ 最新" Style="{StaticResource ToolbarButtonStyle}"
                            Margin="6,0,0,0" Click="BtnSort_Click" ToolTip="切换排序方向"/>
                </Grid>

                <!-- 归档树容器 -->
                <Border Grid.Row="1" Background="#252525" BorderBrush="#333" BorderThickness="1" CornerRadius="4"
                        MouseLeftButtonDown="TreeContainer_MouseLeftButtonDown">
                    <Grid>
                        <!-- 归档树 -->
                        <TreeView x:Name="ArchiveTree" Style="{StaticResource DarkTreeViewStyle}"
                                  ItemContainerStyle="{StaticResource ArchiveTreeViewItemStyle}"
                                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                  MouseDoubleClick="ArchiveTree_MouseDoubleClick"
                                  SelectedItemChanged="ArchiveTree_SelectedItemChanged">
                            <TreeView.ItemTemplate>
                                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <!-- 图标 -->
                                        <TextBlock Grid.Column="0" Text="{Binding Icon}" FontSize="14"
                                                   VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <!-- 标题 -->
                                        <TextBlock Grid.Column="1" Text="{Binding Title}" FontSize="13"
                                                   Foreground="White" TextTrimming="CharacterEllipsis"
                                                   VerticalAlignment="Center"/>
                                        <!-- 时间 -->
                                        <TextBlock Grid.Column="2" Text="{Binding FormattedTime}" FontSize="11"
                                                   Foreground="#666" VerticalAlignment="Center" Margin="10,0,0,0"/>
                                        <!-- 删除按钮 -->
                                        <Button Grid.Column="3" Content="✕" Style="{StaticResource DeleteIconButtonStyle}"
                                                Tag="{Binding Id}" Click="BtnDeleteItem_Click" Margin="6,0,0,0"/>
                                    </Grid>
                                </HierarchicalDataTemplate>
                            </TreeView.ItemTemplate>
                        </TreeView>

                        <!-- 空状态提示 -->
                        <TextBlock x:Name="EmptyHint" Text="暂无归档内容" Foreground="#888" FontSize="14"
                                   HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</helpers:AnimatedWindow>
