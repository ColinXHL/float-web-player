<UserControl x:Class="AkashaNavigator.Views.Pages.InstalledPluginsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:AkashaNavigator.ViewModels.Pages"
             Background="Transparent"
             Cursor="Arrow">
    
    <!-- 已移除本地资源定义，使用全局样式：
         - BoolToVisibilityConverter -> 全局 Converters.xaml
         - LocalPrimaryButtonStyle -> 全局 PrimaryActionButtonStyle
         - LocalDangerButtonStyle -> 全局 DangerActionButtonStyle -->

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题和搜索栏 -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="已安装插件" FontSize="18" FontWeight="SemiBold" 
                       Foreground="White" VerticalAlignment="Center"/>
            <Button x:Name="BtnCheckUpdate" Grid.Column="1" Content="🔄 检查更新"
                    Style="{StaticResource ActionButtonStyle}" Margin="0,0,12,0"
                    Command="{Binding CheckUpdateCommand}"/>
            <TextBox x:Name="SearchBox" Grid.Column="2" Width="200"
                     Style="{StaticResource SearchBoxStyle}" Tag="搜索插件..."
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>

        <!-- 统计信息 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock x:Name="PluginCountText" Text="{Binding PluginCountText}" Foreground="#888" FontSize="12"/>
        </StackPanel>

        <Border Grid.Row="2"
                Margin="0,0,0,16"
                Padding="12,10"
                Background="{StaticResource InfoBackgroundBrush}"
                CornerRadius="5"
                Visibility="{Binding IsPluginUpdateBusy, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel>
                <ProgressBar Height="5"
                             Minimum="0"
                             Maximum="100"
                             Value="{Binding PluginUpdateProgress}"
                             IsIndeterminate="{Binding IsPluginUpdateProgressIndeterminate}"
                             Foreground="{StaticResource InfoBrush}"
                             Background="{StaticResource SurfaceDarkerBrush}"/>
                <TextBlock Text="{Binding PluginUpdateStatusText}"
                           Margin="0,5,0,0"
                           Foreground="{StaticResource InfoLightBrush}"
                           FontSize="11"/>
            </StackPanel>
        </Border>

        <!-- 插件列表 -->
        <ScrollViewer Grid.Row="3" Style="{StaticResource DarkScrollViewer}"
                      VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- 无插件提示 -->
                <TextBlock x:Name="NoPluginsText" Text="没有已安装的插件"
                           Foreground="#666" FontSize="13" Margin="0,20,0,0"
                           HorizontalAlignment="Center"
                           Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibilityConverter}}"/>
                
                <!-- 插件列表 -->
                <ItemsControl x:Name="PluginList" ItemsSource="{Binding Plugins}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource PluginCardStyle}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- 第一行：名称、版本、状态标签 -->
                                    <Grid Grid.Row="0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                                            <TextBlock Text="🔌" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                            <TextBlock Text="{Binding Name}" Foreground="White" 
                                                       FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding Version, StringFormat=' v{0}'}" 
                                                       Foreground="#888" FontSize="12" 
                                                       VerticalAlignment="Center" Margin="8,0,0,0"/>
                                        </StackPanel>
                                        
                                        <!-- 更新可用标签 -->
                                        <Border Grid.Column="1" Style="{StaticResource UpdateAvailableTagStyle}"
                                                Visibility="{Binding UpdateAvailableTagVisibility}" Margin="0,0,8,0">
                                            <TextBlock Text="{Binding UpdateAvailableTagText}" Foreground="#F59E0B" FontSize="11"/>
                                        </Border>
                                        
                                        <!-- 已安装标签 -->
                                        <Border Grid.Column="2" Style="{StaticResource InstalledTagStyle}">
                                            <TextBlock Text="已安装" Foreground="#4ADE80" FontSize="11"/>
                                        </Border>
                                    </Grid>

                                    <!-- 第二行：描述和关联Profile -->
                                    <StackPanel Grid.Row="1" Margin="0,8,0,0">
                                        <TextBlock Text="{Binding Description}" Foreground="#AAA" FontSize="12"
                                                   TextWrapping="Wrap" Visibility="{Binding HasDescription}"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                            <TextBlock Text="关联 Profile: " Foreground="#666" FontSize="11"/>
                                            <TextBlock Text="{Binding ProfilesText}" Foreground="#888" FontSize="11"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <!-- 第三行：操作按钮 -->
                                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,0">
                                        <Button Content="{Binding UpdateButtonText}" Style="{StaticResource PrimaryActionButtonStyle}"
                                                Command="{Binding DataContext.UpdatePluginCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding Id}"
                                                Visibility="{Binding UpdateButtonVisibility}" Margin="0,0,8,0"/>
                                        <Button Content="➕ 添加到 Profile" Style="{StaticResource ActionButtonStyle}"
                                                Command="{Binding DataContext.AddToProfileCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding Id}" Margin="0,0,8,0"/>
                                        <Button Content="🗑 卸载" Style="{StaticResource DangerActionButtonStyle}"
                                                Command="{Binding DataContext.UninstallCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding Id}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
