<helpers:AnimatedWindow x:Class="AkashaNavigator.Views.Dialogs.RecordNoteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:AkashaNavigator.Helpers"
        Title="记录笔记"
        Width="420"
        Height="480"
        WindowStartupLocation="CenterScreen"
        Foreground="White"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="False">

    <helpers:AnimatedWindow.Resources>
        <!-- TreeViewItem 样式 - 包含 EventSetter，需要本地定义 -->
        <Style x:Key="NoteTreeViewItemStyle" TargetType="TreeViewItem" BasedOn="{StaticResource DarkTreeViewItemStyle}">
            <EventSetter Event="PreviewMouseRightButtonDown" Handler="TreeViewItem_PreviewMouseRightButtonDown"/>
        </Style>
    </helpers:AnimatedWindow.Resources>

    <!-- 主容器 -->
    <Border x:Name="MainContainer" Background="#2D2D2D" CornerRadius="8" RenderTransformOrigin="0.5,0.5"
            MouseLeftButtonDown="MainContainer_MouseLeftButtonDown">
        <Border.RenderTransform>
            <ScaleTransform x:Name="ContainerScale" ScaleX="1" ScaleY="1"/>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.6" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>  <!-- 标题栏 -->
                <RowDefinition Height="*"/>     <!-- 内容区 -->
                <RowDefinition Height="Auto"/>  <!-- 按钮区 -->
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Grid.Row="0" Background="#252525" CornerRadius="8,8,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="16,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- 图标 -->
                    <TextBlock Grid.Column="0" Text="💾" FontSize="16"
                               VerticalAlignment="Center" Margin="0,0,10,0"/>

                    <!-- 标题 -->
                    <TextBlock Grid.Column="1" Text="记录笔记" FontSize="14" FontWeight="SemiBold"
                               Foreground="White" VerticalAlignment="Center"/>

                    <!-- 关闭按钮 -->
                    <Button Grid.Column="2" Style="{StaticResource DialogCloseButtonStyle}"
                            Click="BtnClose_Click"/>
                </Grid>
            </Border>

            <!-- 内容区 -->
            <Grid Grid.Row="1" Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>  <!-- 标题输入 -->
                    <RowDefinition Height="Auto"/>  <!-- URL 显示 -->
                    <RowDefinition Height="Auto"/>  <!-- 目录选择标签 -->
                    <RowDefinition Height="*"/>     <!-- 目录树 -->
                    <RowDefinition Height="Auto"/>  <!-- 新建目录 -->
                </Grid.RowDefinitions>

                <!-- 标题输入 -->
                <StackPanel Grid.Row="0" Margin="0,0,0,12">
                    <TextBlock Text="笔记标题" Foreground="#888" FontSize="12" Margin="0,0,0,6"/>
                    <TextBox x:Name="TxtTitle"
                             Style="{StaticResource DarkTextBoxStyle}"
                             Tag="输入笔记标题..."
                             TextChanged="TxtTitle_TextChanged"/>
                    <TextBlock x:Name="TxtError" Foreground="#F87171" FontSize="11"
                               Margin="0,4,0,0" Visibility="Collapsed"/>
                </StackPanel>

                <!-- URL 输入 -->
                <StackPanel Grid.Row="1" Margin="0,0,0,12">
                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="URL" Foreground="#888" FontSize="12"
                                   VerticalAlignment="Center"/>
                        <Button x:Name="BtnGetCurrentUrl" Grid.Column="1" Content="📋 获取当前" 
                                Style="{StaticResource SmallButtonStyle}"
                                Click="BtnGetCurrentUrl_Click" ToolTip="获取当前播放器的 URL"/>
                    </Grid>
                    <TextBox x:Name="TxtUrl"
                             Style="{StaticResource DarkTextBoxStyle}"
                             Tag="输入或粘贴 URL..."
                             TextChanged="TxtUrl_TextChanged"/>
                </StackPanel>

                <!-- 目录选择标签 -->
                <Grid Grid.Row="2" Margin="0,0,0,6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="选择目录（可选）" Foreground="#888" FontSize="12"
                               VerticalAlignment="Center"/>
                    <Button Grid.Column="1" Content="📁 新建目录" Style="{StaticResource SmallButtonStyle}"
                            Click="BtnNewFolder_Click"/>
                </Grid>

                <!-- 目录树 -->
                <Border Grid.Row="3" Background="#252525" CornerRadius="4" Padding="8"
                        MouseLeftButtonDown="FolderTreeContainer_MouseLeftButtonDown">
                    <Grid>
                        <TreeView x:Name="FolderTree" Style="{StaticResource DarkTreeViewStyle}"
                                  ItemContainerStyle="{StaticResource NoteTreeViewItemStyle}"
                                  SelectedItemChanged="FolderTree_SelectedItemChanged"
                                  Background="Transparent">
                            <TreeView.ItemTemplate>
                                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Icon}" FontSize="14" Margin="0,0,6,0"/>
                                        <TextBlock Text="{Binding Name}" FontSize="13" Foreground="White"/>
                                    </StackPanel>
                                </HierarchicalDataTemplate>
                            </TreeView.ItemTemplate>
                        </TreeView>
                        <!-- 空状态提示 -->
                        <TextBlock x:Name="EmptyFolderHint" Text="暂无目录，可点击上方按钮新建"
                                   Foreground="#666" FontSize="12"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   Visibility="Collapsed"/>
                    </Grid>
                </Border>

                <!-- 新建目录输入区（默认隐藏） -->
                <Grid x:Name="NewFolderPanel" Grid.Row="4" Margin="0,8,0,0" Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" MinWidth="100"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="TxtNewFolderName" Grid.Column="0"
                             Style="{StaticResource DarkTextBoxStyle}"
                             Tag="输入目录名称..."
                             KeyDown="TxtNewFolderName_KeyDown"/>
                    <Button Grid.Column="1" Content="✓" Style="{StaticResource SquareButtonStyle}"
                            Margin="6,0,0,0" Click="BtnConfirmNewFolder_Click"/>
                    <Button Grid.Column="2" Content="✕" Style="{StaticResource SquareButtonStyle}"
                            Margin="6,0,0,0" Click="BtnCancelNewFolder_Click"/>
                </Grid>
            </Grid>

            <!-- 按钮区 -->
            <Border Grid.Row="2" Background="#252525" CornerRadius="0,0,8,8" Padding="16,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- 左侧：开荒笔记按钮 -->
                    <Button Grid.Column="0" Content="📂 开荒笔记" 
                            Style="{StaticResource SecondaryButtonStyle}"
                            HorizontalAlignment="Left"
                            Click="BtnPioneerNotes_Click"/>
                    
                    <!-- 右侧：确定和取消按钮 -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="BtnConfirm" Content="确定" Style="{StaticResource PrimaryButtonStyle}"
                                Click="BtnConfirm_Click" Margin="0,0,8,0" IsEnabled="False"/>
                        <Button Content="取消" Style="{StaticResource SecondaryButtonStyle}"
                                Click="BtnCancel_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</helpers:AnimatedWindow>
