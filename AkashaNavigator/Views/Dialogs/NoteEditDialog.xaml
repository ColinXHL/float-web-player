<helpers:AnimatedWindow x:Class="AkashaNavigator.Views.Dialogs.NoteEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:AkashaNavigator.Helpers"
        Title="编辑"
        Width="360"
        Height="200"
        MinHeight="200"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        Foreground="White"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="False">

    <!-- 主容器 -->
    <Border x:Name="MainContainer" Background="#2D2D2D" CornerRadius="8" RenderTransformOrigin="0.5,0.5"
            MouseLeftButtonDown="MainContainer_MouseLeftButtonDown">
        <Border.RenderTransform>
            <ScaleTransform x:Name="ContainerScale" ScaleX="1" ScaleY="1"/>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.6" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>  <!-- 标题栏 -->
                <RowDefinition Height="*"/>     <!-- 内容区 -->
                <RowDefinition Height="Auto"/>  <!-- 按钮区 -->
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Grid.Row="0" Background="#252525" CornerRadius="8,8,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="16,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- 图标 -->
                    <TextBlock Grid.Column="0" Text="✏️" FontSize="14"
                               VerticalAlignment="Center" Margin="0,0,10,0"/>

                    <!-- 标题 -->
                    <TextBlock x:Name="TitleText" Grid.Column="1" Text="编辑" FontSize="14" FontWeight="SemiBold"
                               Foreground="White" VerticalAlignment="Center"/>

                    <!-- 关闭按钮 -->
                    <Button Grid.Column="2" Style="{StaticResource DialogCloseButtonStyle}"
                            Click="BtnClose_Click"/>
                </Grid>
            </Border>

            <!-- 内容区 -->
            <Grid Grid.Row="1" Margin="16,12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 提示文本 -->
                <TextBlock x:Name="PromptText" Grid.Row="0" Text="请输入新名称：" 
                           Foreground="#888" FontSize="12" Margin="0,0,0,8"/>

                <!-- 输入框 -->
                <TextBox x:Name="TxtInput" Grid.Row="1"
                         Style="{StaticResource DarkTextBoxStyle}"
                         TextChanged="TxtInput_TextChanged" KeyDown="TxtInput_KeyDown"/>

                <!-- URL 提示文本（可选） -->
                <Grid x:Name="UrlPromptGrid" Grid.Row="2" Margin="0,12,0,8" Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="UrlPromptText" Grid.Column="0" Text="URL：" 
                               Foreground="#888" FontSize="12" VerticalAlignment="Center"/>
                    <Button x:Name="BtnGetCurrentUrl" Grid.Column="1" Content="📋 获取当前" 
                            Style="{StaticResource SecondaryButtonStyle}"
                            Padding="8,4" MinWidth="0" FontSize="11"
                            Click="BtnGetCurrentUrl_Click" ToolTip="获取当前播放器的 URL"/>
                </Grid>

                <!-- URL 输入框（可选） -->
                <TextBox x:Name="TxtUrl" Grid.Row="3"
                         Style="{StaticResource DarkTextBoxStyle}"
                         TextChanged="TxtUrl_TextChanged" Visibility="Collapsed"/>
            </Grid>

            <!-- 按钮区 -->
            <Border Grid.Row="2" Background="#252525" CornerRadius="0,0,8,8" Padding="16,12">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="BtnConfirm" Content="确定" Style="{StaticResource PrimaryButtonStyle}"
                            Click="BtnConfirm_Click" Margin="0,0,8,0" IsEnabled="False"/>
                    <Button Content="取消" Style="{StaticResource SecondaryButtonStyle}"
                            Click="BtnCancel_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</helpers:AnimatedWindow>
